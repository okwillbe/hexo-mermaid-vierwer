{
  "name": "hexo-mermaid-viewer",
  "version": "1.0.0",
  "description": "Plugin for rendering Mermaid diagrams",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mermaid": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}